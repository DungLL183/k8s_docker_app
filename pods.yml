apiVersion: v1
kind: Pod
metadata:
  name: nodeapp
  labels:
    app: nodeapp
spec:
  containers:
    - name: nodeapp
      image: dungll183/my-docker-app:tagVersion
      ports:
        - containerPort: 5000
